name: Master branch updater.

on:
  release:
    types: released

jobs:
  User-agent:
    if: false
    runs-on: ubuntu-slim
    steps:
      - uses: desktop-app/action_code_updater@master
        with:
          type: "dev-to-master"
